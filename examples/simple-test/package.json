{
  "name": "simple-test",
  "private": true,
  "type": "module",
  "scripts": {
    "start:bun": "bun run src/bun.mts",
    "start:bun-fastify": "bun run src/fastify.mts",
    "start:fastify": "yarn tsx src/fastify.mts"
  },
  "dependencies": {
    "@navios/builder": "workspace:*",
    "@navios/core": "workspace:*",
    "@navios/di": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.21.0"
  }
}
