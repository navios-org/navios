{
  "name": "otel-fastify",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx src/bootstrap.mts",
    "start:watch": "tsx watch src/bootstrap.mts",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f"
  },
  "dependencies": {
    "@navios/adapter-fastify": "workspace:*",
    "@navios/builder": "workspace:*",
    "@navios/core": "workspace:*",
    "@navios/di": "workspace:*",
    "@navios/otel": "workspace:*",
    "@navios/otel-fastify": "workspace:*",
    "@opentelemetry/api": "^1.9.0",
    "dotenv": "^17.2.3",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
